import { type Route } from '@angular/router';
import { ShellComponent } from '@legislative-tracker/portal-web-features-shell';

export const appRoutes: Route[] = [{ path: '', component: ShellComponent }];
