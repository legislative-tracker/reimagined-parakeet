import { fetchMembers } from '@legislative-tracker/plugin-leg-us-ny';
import { fetchLegislators } from '@legislative-tracker/plugin-openstates';

export const syncLegislators = async (stateCd: string) => {
  return [];
};
