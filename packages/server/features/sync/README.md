# server-feature-sync

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build server-feature-sync` to build the library.

## Running unit tests

Run `nx test server-feature-sync` to execute the unit tests via [Vitest](https://vitest.dev/).
