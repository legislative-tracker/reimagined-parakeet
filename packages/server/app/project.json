{
  "name": "server-app",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "tags": [
    "firebase:app",
    "firebase:name:server-app",
    "platform:server",
    "type:app"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "options": {
        "command": "echo Build succeeded."
      }
    },
    "watch": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nx run-many --targets=build --projects=tag:firebase:dep:server-app --parallel=100 --watch"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nx run-many --targets=lint --projects=tag:firebase:dep:server-app --parallel=100"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nx run-many --targets=typecheck --projects=tag:firebase:dep:server-app --parallel=100"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nx run-many --targets=test --projects=tag:firebase:dep:server-app --parallel=100"
      }
    },
    "firebase": {
      "executor": "nx:run-commands",
      "options": {
        "command": "firebase --config=firebase.json"
      },
      "configurations": {
        "production": {
          "command": "firebase --config=firebase.json"
        }
      }
    },
    "killports": {
      "executor": "nx:run-commands",
      "cache": false,
      "options": {
        "command": "kill-port --port 9099,5001,8080,9000,5000,8085,9199,9299,4000,4400,4500"
      }
    },
    "getconfig": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nx run server-app:firebase functions:config:get > packages/server/app/environment/.runtimeconfig.json"
      }
    },
    "emulate": {
      "executor": "nx:run-commands",
      "dependsOn": ["killports"],
      "options": {
        "command": "nx run server-app:firebase emulators:start --only auth,functions,firestore,pubsub,eventarc --import=packages/server/app/.emulators --export-on-exit"
      }
    },
    "serve": {
      "executor": "@simondotm/nx-firebase:serve",
      "options": {
        "commands": ["nx run server-app:watch", "nx run server-app:emulate"]
      }
    },
    "deploy": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "nx run server-app:firebase deploy"
      }
    }
  },
  "implicitDependencies": ["server-triggers"]
}
