# server-shared-utils

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build server-shared-utils` to build the library.

## Running unit tests

Run `nx test server-shared-utils` to execute the unit tests via [Vitest](https://vitest.dev/).
