# plugin-shared-utils

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build plugin-shared-utils` to build the library.

## Running unit tests

Run `nx test plugin-shared-utils` to execute the unit tests via [Vitest](https://vitest.dev/).
