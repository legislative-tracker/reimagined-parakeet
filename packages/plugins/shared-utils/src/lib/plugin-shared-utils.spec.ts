import { pluginSharedUtils } from './plugin-shared-utils';

describe('pluginSharedUtils', () => {
  it('should work', () => {
    expect(pluginSharedUtils()).toEqual('plugin-shared-utils');
  });
});
