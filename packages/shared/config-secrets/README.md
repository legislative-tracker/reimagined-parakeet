# shared-config-secrets

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-config-secrets` to build the library.

## Running unit tests

Run `nx test shared-config-secrets` to execute the unit tests via [Vitest](https://vitest.dev/).
